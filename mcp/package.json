{
  "name": "forge-mcp",
  "version": "0.50.0",
  "private": true,
  "description": "Atomadic Forge — MCP Streamable HTTP server on Cloudflare Workers",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250429.0",
    "typescript": "^5.4.5",
    "wrangler": "^4.76.0"
  }
}
